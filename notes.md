git add .
git commit -m "03: database setup"
git checkout -b 03:database-setup
git push -u origin 03:database-setup

go to firecrawl to see how to install firecrawl
and then go to inggest function to create a function
